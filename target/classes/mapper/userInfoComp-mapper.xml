<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.xupt.mapper.UserMapper">
    <update id="userInfoComplete" parameterType="User">
        UPDATE stu_login SET school_id = #{school_id},major = #{major},stu_nickname = #{stu_nickname},head = #{head},signature = #{signature} WHERE stu_num = #{stu_num};
    </update>
</mapper>

